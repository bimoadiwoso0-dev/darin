/**
 * پیکربندی تست‌های یکپارچه.
 *
 * ── چرا یکپارچه و نه واحد (Unit) ─────────────────────────────────────────
 * ارزش این سامانه در تضمین‌هایی است که فقط با دیتابیس واقعی قابل اثبات‌اند:
 * ایندکس یکتای جزئی که مانع امانت هم‌زمان یک نسخه می‌شود، تابع
 * `persian_normalize` که باید با نسخه TypeScript دقیقاً یکسان باشد، و
 * CHECK constraint هایی که داده نامعتبر را رد می‌کنند. تست واحد با Mock
 * هیچ‌کدام را نمی‌سنجد؛ فقط ثابت می‌کند کد ما خودش را صدا می‌زند.
 *
 * ── چرا `runInBand` ──────────────────────────────────────────────────────
 * تست‌ها روی یک دیتابیس مشترک اجرا می‌شوند. اجرای موازی یعنی یک تست،
 * داده تست دیگر را می‌بیند یا پاک می‌کند.
 */
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  rootDir: '.',
  testMatch: ['<rootDir>/test/**/*.spec.ts'],
  /*
   * `setupFiles` و نه `setupFilesAfterEnv`.
   *
   * اعتبارسنجی متغیرهای محیطی هنگام **وارد کردن** `AppModule` اجرا می‌شود،
   * یعنی پیش از اجرای هر تست. `setupFilesAfterEnv` دیرتر از آن اجرا می‌شود
   * و متغیرها هنوز تنظیم نشده‌اند.
   */
  setupFiles: ['<rootDir>/test/setup.ts'],
  moduleNameMapper: {
    /*
     * حذف پسوند از Import های نسبی.
     *
     * دو منبع چنین Import هایی دارند: Prisma Client تولیدشده (`.ts`) و
     * بسته مشترک که به سبک ESM نوشته شده (`.js`). حل‌کننده CommonJS جِست
     * هیچ‌کدام را نمی‌شناسد.
     */
    '^(\\.{1,2}/.*)\\.(ts|js)$': '$1',
    /*
     * بسته مشترک به‌صورت ESM ساخته می‌شود و Jest که CommonJS اجرا می‌کند
     * نمی‌تواند آن را بخواند. به‌جای افزودن یک خروجی CJS فقط برای تست،
     * مستقیم به سورس TypeScript اشاره می‌کنیم تا ts-jest همان‌جا ترجمه‌اش
     * کند — و تست دقیقاً همان کدی را می‌سنجد که در برنامه اجرا می‌شود.
     */
    '^@darin/shared$': '<rootDir>/../../packages/shared/src/index.ts',
  },
  transform: {
    '^.+\\.ts$': ['ts-jest', { tsconfig: '<rootDir>/tsconfig.json', isolatedModules: true }],
  },
  testTimeout: 30_000,
  maxWorkers: 1,
};
