# وضعیت نهایی پروژه

این سند می‌گوید **چه چیزی ساخته شده، کجاست، و چطور بررسی شده** — و به همان
اندازه مهم، **چه چیزی ساخته نشده**.

---

## در یک نگاه

| | |
|---|---|
| ماژول API | ۲۱ |
| Endpoint | ۱۶۶ |
| صفحه رابط کاربری | ۳۲ صفحه · ۷۱ مسیر |
| جدول پایگاه داده | ۴۲ مدل · ۳۲ Enum · ۴ مهاجرت |
| مجوز ریزدانه | ۵۲ در ۶ نقش سیستمی |
| آزمون | ۴۹ آزمون یکپارچه API + ۷۱ آزمون واحد بسته مشترک |
| بررسی ایستا | `tsc` سخت‌گیرانه روی هر سه بسته + ESLint نوع‌آگاه، بدون خطا |
| بزرگ‌ترین مقیاس سنجیده‌شده | ۱۰۰٬۰۰۰ کتاب · ۱۲۲٬۱۵۲ نسخه · ۲۴۲ مگابایت |
| خط کد | ~۳۸٬۰۰۰ (بدون کد تولیدشده) |

---

## ۱. کاتالوگ

| خواسته | وضعیت | کجا |
|--------|-------|-----|
| رکورد کتاب‌شناختی جدا از نسخه فیزیکی | ✅ | `Book` ↔ `BookCopy` — ADR-01 در `docs/ARCHITECTURE.md` |
| نویسنده، مترجم، ویراستار با نقش | ✅ | `Person` + `BookContributor.role` |
| ناشر، مجموعه، درخت دسته‌بندی، برچسب | ✅ | `modules/catalog/reference.*` |
| کتاب چندجلدی | ✅ | `parentBookId` + `volumeNumber` |
| شماره‌گذاری قابل تنظیم | ✅ | `modules/numbering` — قالب از تنظیمات، شمارنده داخل تراکنش |
| ISBN با اعتبارسنجی و تبدیل ۱۰↔۱۳ | ✅ | `packages/shared/src/domain/isbn.ts` |
| تشخیص کتاب تکراری | ✅ | `POST /books/check-duplicate` — شابک + شباهت trigram عنوان |
| بارکد و QR و چاپ برچسب | ✅ | `modules/labels` — تصویر واقعی با `bwip-js` و `qrcode` |
| ویرایش گروهی | ✅ | `POST /books/bulk-update` |

## ۲. موجودی فیزیکی و مکان

| خواسته | وضعیت | کجا |
|--------|-------|-----|
| درخت مکان (ساختمان ← قفسه ← طبقه) | ✅ | Materialized Path — ADR-03 |
| جابه‌جایی کتاب با تاریخچه | ✅ | `BookMovement` |
| نقشه بصری اشغال قفسه | ✅ | `GET /locations/occupancy` + `LocationsPage` |
| وضعیت ظاهری و منبع تهیه هر نسخه | ✅ | `BookCopy.condition` · `acquisitionSource` · `donorId` |
| شمارش موجودی با بارکد | ✅ | `modules/inventory` — جلسه قابل توقف و ادامه |
| اصلاح گروهی محل پس از شمارش | ✅ | `POST /inventory/:id/apply-locations` |

## ۳. اعضا

| خواسته | وضعیت | کجا |
|--------|-------|-----|
| انواع عضویت با قوانین متفاوت | ✅ | `MembershipType` |
| اعتبارسنجی کد ملی ایرانی | ✅ | `isValidIranianNationalId` |
| کارت عضویت با بارکد و QR | ✅ | `GET /members/:id/card` + چاپ گروهی |
| انقضا و تمدید عضویت | ✅ | `POST /members/:id/renew` + علامت‌گذاری شبانه |
| محرمانگی اطلاعات شخصی | ✅ | جدول صریح در `docs/SECURITY.md` §۵ |

## ۴. امانت

| خواسته | وضعیت | کجا |
|--------|-------|-----|
| امانت، بازگشت، تمدید | ✅ | `modules/circulation` |
| بررسی شرایط پیش از ثبت | ✅ | `GET /loans/eligibility/:memberId` |
| رزرو با صف نوبت | ✅ | `Reservation.queuePosition` |
| کنارگذاری خودکار نسخه هنگام بازگشت | ✅ | `holdCopyId` |
| جریمه، پرداخت، بخشش | ✅ | `Fine` + `Payment` |
| گزارش مفقودی | ✅ | `LostReport` |
| میز امانت سریع | ✅ | `CirculationDeskPage` — `F4` |
| **جلوگیری از امانت هم‌زمان یک نسخه** | ✅ | دو لایه: به‌روزرسانی شرطی + ایندکس یکتای جزئی `loans_one_open_per_copy`. آزمون: `test/circulation.spec.ts` |

## ۵. جستجو

| خواسته | وضعیت | کجا |
|--------|-------|-----|
| نرمال‌سازی فارسی (ي/ی، ك/ک، اعراب، نیم‌فاصله، ارقام) | ✅ | تابع `persian_normalize()` در پایگاه داده + همتای دقیق در TypeScript |
| جستجوی تمام‌متن | ✅ | `tsvector` + ایندکس GIN |
| تحمل غلط املایی | ✅ | `pg_trgm` |
| فیلتر ترکیبی | ✅ | `GET /books` |
| جستجوی سراسری بین موجودیت‌ها | ✅ | `GET /search/global` |
| فیلترها در نشانی صفحه | ✅ | `useUrlFilters` — هر نما قابل اشتراک و Bookmark |

## ۶. گزارش و داشبورد

| خواسته | وضعیت | کجا |
|--------|-------|-----|
| آمار واقعی، بدون عدد ثابت | ✅ | `modules/reports` — هر عدد یک کوئری |
| نمودار روند | ✅ | `GET /dashboard/trend` |
| خروجی Excel و CSV واقعی | ✅ | `ExcelJS` — برچسب‌های فارسی، تاریخ شمسی |
| گزارش‌های آماده | ✅ | `GET /reports` |

## ۷. ورود اطلاعات

| خواسته | وضعیت | کجا |
|--------|-------|-----|
| آپلود Excel و تشخیص ستون‌ها | ✅ | `POST /imports/upload` |
| نگاشت ستون به فیلد | ✅ | `POST /imports/:id/mapping` |
| **اعتبارسنجی بدون نوشتن** | ✅ | `POST /imports/:id/validate` — گزارش خطای سطر به سطر |
| اجرای واقعی | ✅ | `POST /imports/:id/execute` |

## ۸. کاربران و امنیت

| خواسته | وضعیت | کجا |
|--------|-------|-----|
| هیچ رمزی در کد Hard-code نشده | ✅ | مدیر در جادوگر راه‌اندازی ساخته می‌شود |
| اسرار فقط از `.env` | ✅ | `.env.example` + اعتبارسنجی Zod هنگام بالا آمدن |
| RBAC ریزدانه | ✅ | ۵۲ مجوز، ۶ نقش، نقش دلخواه |
| argon2id | ✅ | پارامترهای توصیه OWASP |
| قفل شدن حساب پس از تلاش ناموفق | ✅ | ۵ تلاش → ۱۵ دقیقه |
| توکن چرخشی با تشخیص استفاده مجدد | ✅ | باطل شدن کل خانواده توکن |
| محدودیت نرخ، سخت‌گیرانه‌تر روی ورود | ✅ | `LoginThrottlerGuard` |
| Audit Log کامل | ✅ | `modules/audit` — فقط خواندنی |
| **کاربر Stack Trace نمی‌بیند** | ✅ | فیلتر سراسری خطا؛ پیام فارسی + شناسه درخواست، جزئیات در Log |
| محافظ آخرین مدیر ارشد | ✅ | `assertNotLastSuperAdmin` |
| حذف نرم | ✅ | `deletedAt` روی موجودیت‌های حساس |

## ۹. رابط کاربری

| خواسته | وضعیت | کجا |
|--------|-------|-----|
| **RTL واقعی** | ✅ | `dir="rtl"` روی `<html>` + ویژگی‌های منطقی CSS در سراسر کد |
| تاریخ شمسی در نمایش، استاندارد در ذخیره‌سازی | ✅ | `docs/DESIGN_SYSTEM.md` §تاریخ |
| تقویم شمسی در فیلد تاریخ | ✅ | `JalaliDateInput` |
| حالت شب | ✅ | نشانه‌های معنایی رنگ؛ انتخاب کاربر ذخیره می‌شود |
| میان‌بر صفحه‌کلید | ✅ | `F2`–`F8` و `؟` — `docs/USER_GUIDE.md` |
| ثبت کتاب با گفتار فارسی | ✅ | دیکته رکورد کامل + میکروفون هر فیلد. تجزیه بر پایه کلیدواژه، آفلاین و قابل پیش‌بینی؛ نتیجه پیش از اعمال نشان داده می‌شود |
| حالت بارگذاری، خالی و خطا | ✅ | `Skeleton`، `EmptyState` در همه فهرست‌ها |
| چاپ تمیز | ✅ | `usePrintArea` + قواعد `@media print` |
| واکنش‌گرا | ✅ | نوار کناری تاشو، جدول‌های افقی‌اسکرول |

## ۱۰. عملیات

| خواسته | وضعیت | کجا |
|--------|-------|-----|
| **پشتیبان‌گیری واقعی** | ✅ | `pg_dump` فشرده با چک‌سام — نه شبیه‌سازی |
| پشتیبان‌گیری زمان‌بندی‌شده | ✅ | شبانه، با نگهداری قابل تنظیم |
| بازیابی با محافظ | ✅ | تأیید متنی + بررسی چک‌سام + پشتیبان ایمنی خودکار |
| کارهای نگهداری شبانه | ✅ | `modules/maintenance` — دیرکرد، انقضا، یادآوری |
| Health Check واقعی | ✅ | `/api/health/ready` واقعاً به پایگاه داده کوئری می‌زند |
| Log ساختاریافته | ✅ | Pino با شناسه درخواست |
| استقرار با Docker | ✅ | `docker-compose.yml` + دو Dockerfile |

## ۱۱. کارایی

سنجیده روی **۱۰۰٬۰۰۰ کتاب · ۱۲۲٬۱۵۲ نسخه · ۱۷٬۹۵۶ امانت (۲۴۲ مگابایت)**.
میانه پنج اجرا:

| کوئری | زمان | سقف |
|-------|------|-----|
| یافتن نسخه با بارکد | ۲.۴ms | ۵۰ms |
| فهرست کتاب‌ها، صفحه اول | ۵.۰ms | ۲۰۰ms |
| فهرست کتاب‌ها، offset ۵۰۰۰ | ۲.۵ms | ۴۰۰ms |
| جستجوی متن فارسی | ۳۱.۸ms | ۲۰۰ms |
| جستجوی تقریبی trigram | ۱۷۲ms | ۳۰۰ms |
| امانت‌های دیرکرددار با اطلاعات عضو و کتاب | ۱۲.۹ms | ۳۰۰ms |
| آمار داشبورد | ۱۷.۱ms | ۴۰۰ms |
| گزارش امانت‌های جاری | ۳.۲ms | ۵۰۰ms |

بازتولید: `pnpm seed:perf && pnpm --filter @darin/api perf:measure`
(با کد خطا خارج می‌شود اگر کوئری از سقفش بگذرد — قابل استفاده در CI).

---

## آنچه **ساخته نشده**

این‌ها عمداً بیرون از MVP نگه داشته شدند تا پروژه بیش از حد بزرگ و پرریسک
نشود. هیچ‌کدام دکمه یا صفحه بی‌کار در رابط کاربری ندارند.

| قابلیت | وضعیت واقعی امروز |
|--------|-------------------|
| **ارسال پیامک و ایمیل** | جدول `Notification` با ستون `channel`، قالب‌های پیام، و تولید خودکار شبانه یادآوری‌ها هست. **ارسال** نیست: کتابدار از صفحه «یادآوری‌ها» دستی تماس می‌گیرد |
| **کاتالوگ عمومی (OPAC)** | هیچ نمای بدون احراز هویت وجود ندارد. افزودنش نیاز به نمای جداگانه با فهرست صریح فیلدهای مجاز دارد |
| **پنل اعضا و رزرو آنلاین** | عضو حساب کاربری ندارد. رزرو را کتابدار ثبت می‌کند |
| **اپ موبایل** | API کامل و `tokenMode: "bearer"` آماده است؛ اپی نوشته نشده |
| **چند شعبه‌ای کامل** | `branchId` روی همه موجودیت‌ها و جدول `Transfer` هست؛ رابط کاربری انتخاب شعبه و گردش انتقال نیست |
| **جستجوی معنایی / برداری** | جستجوی فعلی FTS + trigram است. `pgvector` نصب نشده |
| **هوش مصنوعی** | چیزی ساخته نشده |
| **دریافت خودکار اطلاعات از ISBN** | اعتبارسنجی و تبدیل ISBN هست؛ اتصال به سرویس بیرونی نیست |
| **آفلاین / PWA کامل** | `vite-plugin-pwa` پیکربندی شده و Service Worker ساخته می‌شود؛ صف عملیات آفلاین نیست |

نقشه هر کدام در [`docs/ROADMAP.md`](docs/ROADMAP.md).

---

## آنچه در این محیط قابل بررسی نبود

صادقانه گفته می‌شود تا کسی به آن تکیه نکند:

| مورد | چرا | چه چیزی به‌جایش بررسی شد |
|------|-----|--------------------------|
| `docker compose build` | CDN لایه‌های Docker Hub از شبکه این محیط در دسترس نیست | چیدمان تصویر Production عیناً شبیه‌سازی شد: نصب فقط با وابستگی‌های Production، سپس مهاجرت، Seed هسته، بوت سرویس، تکمیل Setup، ورود، و ساخت پشتیبان واقعی. **دو اشکال واقعی همین‌جا پیدا شد** (نبود `tsx` و `dotenv` در تصویر). `docker compose config` معتبر است |
| رفتار زیر بار هم‌زمان واقعی | ابزار بارگذاری در دسترس نبود | امانت هم‌زمان با آزمون یکپارچه و ایندکس یکتای پایگاه داده بررسی شد |
| چاپ روی چاپگر فیزیکی | چاپگری وجود ندارد | پیش‌نمایش چاپ مرورگر بررسی شد |
| تبدیل واقعی صدا به متن | میکروفون و دسترسی به سرویس تشخیص گفتار وجود ندارد | کل زنجیره پس از تبدیل — قلاب، پنل، تجزیه‌گر و پر شدن فرم — با یک موتور تشخیص گفتار جعلی در مرورگر واقعی آزموده شد؛ خودِ تبدیل صدا کار مرورگر است. تجزیه‌گر ۲۹ آزمون واحد دارد |

## بدهی فنی شناخته‌شده

- **۱۲ مورد `react-hooks/set-state-in-effect`.** همگی یک الگو: همگام کردن
  مقدار بیرونی با وضعیت محلی (فرمی که پس از رسیدن داده پر می‌شود،
  انتخابگری که با پاک شدن فیلتر خالی می‌شود). وابستگی‌هایشان درست است و
  رفتارشان در مرورگر بررسی شده، ولی الگوی توصیه‌شده React Compiler نیست.
  به‌صورت هشدار در خروجی lint دیده می‌شوند.

---

## ردیابی خواسته‌ها در کد

کد به شماره خواسته‌ها ارجاع می‌دهد. برای پیدا کردن جای پیاده‌سازی هر
خواسته:

```bash
grep -rn "قانون ۷۹" apps packages
```

۱۰۲ شماره از ۱۵۰ خواسته مستقیماً در توضیحات کد ارجاع داده شده‌اند:

> ۱–۲، ۴، ۶–۱۷، ۱۹–۴۴، ۴۶، ۵۰–۶۴، ۶۷–۶۸، ۷۱، ۷۳، ۷۵–۷۶، ۷۸–۷۹، ۸۱،
> ۸۳–۸۵، ۸۹–۹۶، ۹۸–۹۹، ۱۰۴–۱۱۲، ۱۱۴–۱۱۶، ۱۱۸–۱۲۳، ۱۲۵–۱۲۶، ۱۳۴–۱۳۵، ۱۴۹

بقیه یا خواسته‌های فرایندی‌اند (مثل «فاز به فاز بساز» و «با Git مدیریت
کن») که در خود روند اجرا رعایت شده‌اند، یا در سطح معماری اعمال شده‌اند و
جایشان در `docs/ARCHITECTURE.md` است.

---

## اشکال‌هایی که در همین مرحله پیدا و رفع شدند

مواردی که آزمون و سنجش واقعی بیرون کشید — نه فرضی:

| اشکال | اثر | رفع |
|-------|-----|-----|
| `Button` نوع پیش‌فرض `submit` داشت | هر دکمه فرعی داخل فرم (افزودن پدیدآورنده، پاک کردن، میکروفون) فرم را ثبت می‌کرد | پیش‌فرض `type="button"`؛ دکمه‌های ثبت از قبل صریح بودند |
| افزونه داخل فیلد `ltr` با ویژگی منطقی جای می‌گرفت | دکمه میکروفون روی ارقام شابک می‌افتاد | ویژگی فیزیکی برای فیلدی که هرگز برنمی‌گردد |
| سرصفحه فایل Excel از مسیر تبدیل امن سلول رد نمی‌شد | سرصفحه قالب‌بندی‌شده (که در فایل‌های واقعی رایج است) نام ستون «[object Object]» می‌گرفت و آن ستون قابل نگاشت نبود | همان `cellToString` ردیف‌های داده |
| `cellToString` برای شکل ناشناخته «[object Object]» می‌داد | این رشته از اعتبارسنجی رد می‌شد و می‌توانست عنوان کتاب شود | رشته خالی — در گزارش اعتبارسنجی دیده و اصلاح می‌شود |
| `_count` رابطه‌ای Prisma کل جدول نسخه‌ها را گروه‌بندی می‌کرد | فهرست کتاب‌ها روی ۱۲۲٬۰۰۰ نسخه ۱۶۶ms | `groupBy` روی همان صفحه → ۵.۰ms |
| متن یادآوری با منطقه زمانی سرور ساخته می‌شد | موعدهای نزدیک نیمه‌شب یک روز اشتباه به عضو گفته می‌شد | `formatJalaliDate` با منطقه زمانی صریح |
| `tsx` و `dotenv` وابستگی توسعه بودند | استقرار تازه بدون نقش و مجوز بالا می‌آمد و Setup کار نمی‌کرد | ترجمه Seed به JavaScript + انتقال `dotenv` |
| اعلان‌ها ساخته می‌شدند ولی هیچ‌کس نمی‌خواند | قابلیت نیمه‌ساخته | صفحه و API «یادآوری‌ها» |
| `notifications.manage` به هیچ نقشی داده نشده بود | «ناظر گزارش‌ها» می‌توانست وضعیت را تغییر دهد | تفکیک مجوز خواندن از نوشتن |
| `hasCopies=false` نادیده گرفته می‌شد | فیلتر برعکس عمل می‌کرد | `booleanQuery` صریح |
| `RATE_LIMIT_LOGIN_MAX` مستند بود ولی خوانده نمی‌شد | تنظیم تزئینی | `LoginThrottlerGuard` |
| توکن دسترسی به‌صورت پیش‌فرض در بدنه پاسخ بود | سطح حمله بی‌دلیل | فقط با `tokenMode: "bearer"` |
| دو اشکال در تبدیل تاریخ شمسی | تاریخ غلط در ۳۰ اسفند سال کبیسه | آزمون رفت‌وبرگشت ۲۱ ساله |
